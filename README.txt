This presentation uses https://github.com/schacon/showoff

To view it, "gem install showoff" and run "showoff serve" in this directory,
then visit http://localhost:9090 in your browser.

Or run "showoff static" to generate a static-HTML version.

Alternatively, you can just read the .md files in "title", "virtualenv",
"pip", and "outro".
